# SEO Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add full Thai-targeted SEO to kiddogame.net — dynamic page titles, meta description, Open Graph tags, JSON-LD structured data, sitemap.xml, robots.txt, and a visually-hidden Thai H1 on the home page.

**Architecture:** `SeoMeta.cs` static helper holds Thai keyword strings and JSON-LD builders. `SeoHead.razor` (new shared component) receives pre-built strings and renders all `<head>` tags via Blazor's `<PageTitle>` + `<HeadContent>`. `Home.razor` and `GamePage.razor` call `<SeoHead>` with values from `SeoMeta`. A minimal API endpoint in `Program.cs` serves `/sitemap.xml` using a new `GetAllSlugsAsync()` on `GameService`.

**Tech Stack:** Blazor Server (.NET 8), xUnit, ASP.NET Core minimal API, Supabase PostgREST, C# 11 raw string literals, `System.Text.Json`

---

### Task 1: SeoMeta.cs — Thai keyword strings + JSON-LD builders

**Files:**
- Create: `webapp/Services/SeoMeta.cs`
- Create: `webapp.Tests/SeoMetaTests.cs`

- [ ] **Step 1: Write the failing tests**

```csharp
// webapp.Tests/SeoMetaTests.cs
using Kiddo.Web.Models;
using Kiddo.Web.Services;

namespace Kiddo.Web.Tests;

public class SeoMetaTests
{
    private static Game MakeGame(
        string title   = "Bubble Pop",
        string slug    = "bubble-pop",
        string? descTh = "เกมสนุกสำหรับเด็ก",
        string? descEn = "A fun bubble game") => new()
    {
        Title = title, Slug = slug,
        DescriptionTh = descTh, Description = descEn,
        ObjectId = "abc123"
    };

    [Fact]
    public void GameTitle_ReturnsThaiSuffix()
    {
        var result = SeoMeta.GameTitle(MakeGame("Bubble Pop"));
        Assert.Equal("Bubble Pop - เล่นออนไลน์ฟรี | เกมเด็ก KiddoGame", result);
    }

    [Fact]
    public void GameDescription_PrefersThaiDescription()
    {
        var result = SeoMeta.GameDescription(MakeGame(descTh: "เกมสนุกสำหรับเด็ก"));
        Assert.StartsWith("เกมสนุกสำหรับเด็ก", result);
        Assert.Contains("ไม่ต้องดาวน์โหลด", result);
    }

    [Fact]
    public void GameDescription_FallsBackToEnglish_WhenNoThai()
    {
        var result = SeoMeta.GameDescription(MakeGame(descTh: null, descEn: "A fun bubble game"));
        Assert.StartsWith("A fun bubble game", result);
        Assert.Contains("ไม่ต้องดาวน์โหลด", result);
    }

    [Fact]
    public void GameDescription_FallsBackToKeywords_WhenNoBothDescriptions()
    {
        var result = SeoMeta.GameDescription(MakeGame("My Game", descTh: null, descEn: null));
        Assert.Contains("My Game", result);
        Assert.Contains("ไม่ต้องดาวน์โหลด", result);
    }

    [Fact]
    public void GameDescription_TruncatesLongDesc_At100Chars()
    {
        var longDesc = new string('ก', 200);
        var result = SeoMeta.GameDescription(MakeGame(descTh: longDesc));
        Assert.StartsWith(new string('ก', 100) + " เล่น", result);
    }

    [Fact]
    public void HomeJsonLd_ContainsWebSiteSchemaAndSearchAction()
    {
        var result = SeoMeta.HomeJsonLd();
        Assert.Contains("\"@type\": \"WebSite\"", result);
        Assert.Contains("kiddogame.net", result);
        Assert.Contains("SearchAction", result);
    }

    [Fact]
    public void GameJsonLd_ContainsSoftwareApplicationAndFreeOffer()
    {
        var game = MakeGame("Bubble Pop", slug: "bubble-pop");
        var result = SeoMeta.GameJsonLd(game, "https://kiddogame.net/games/bubble-pop");
        Assert.Contains("\"@type\": \"SoftwareApplication\"", result);
        Assert.Contains("Bubble Pop", result);
        Assert.Contains("https://kiddogame.net/games/bubble-pop", result);
        Assert.Contains("\"price\": \"0\"", result);
    }
}
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
dotnet test webapp.Tests/ --filter "FullyQualifiedName~SeoMetaTests"
```

Expected: FAIL — `SeoMeta` type does not exist

- [ ] **Step 3: Create SeoMeta.cs**

```csharp
// webapp/Services/SeoMeta.cs
using System.Text.Encodings.Web;
using System.Text.Json;
using Kiddo.Web.Models;

namespace Kiddo.Web.Services;

public static class SeoMeta
{
    public const string BaseUrl        = "https://kiddogame.net";
    public const string SiteName       = "KiddoGame";
    public const string DefaultOgImage = "https://kiddogame.net/images/og-default.png";

    public const string HomeTitle =
        "KiddoGame - เกมออนไลน์สำหรับเด็กฟรี 2,000+ เกม ไม่ต้องดาวน์โหลด";

    public const string HomeDescription =
        "รวมเกมออนไลน์สำหรับเด็กฟรีกว่า 2,000 เกม เล่นได้เลยบนเบราว์เซอร์ " +
        "ไม่ต้องดาวน์โหลด ครอบคลุมทุกประเภท เกมการศึกษา เกมปริศนา เกมแข่งรถ " +
        "เกมแต่งตัว เกมทำอาหาร ปลอดภัยสำหรับเด็กทุกวัย";

    public const string HomeH1 =
        "เกมออนไลน์สำหรับเด็กฟรี เล่นได้เลยไม่ต้องดาวน์โหลด";

    private static readonly JsonSerializerOptions _jsonOpts = new()
    {
        Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping
    };

    private static string Esc(string v) =>
        JsonSerializer.Serialize(v, _jsonOpts)[1..^1];

    public static string GameTitle(Game g) =>
        $"{g.Title} - เล่นออนไลน์ฟรี | เกมเด็ก {SiteName}";

    public static string GameDescription(Game g)
    {
        var text = !string.IsNullOrEmpty(g.DescriptionTh) ? g.DescriptionTh
                 : !string.IsNullOrEmpty(g.Description)   ? g.Description
                 : "";
        var snippet = text.Length > 100 ? text[..100] : text;
        return string.IsNullOrEmpty(snippet)
            ? $"เล่น {g.Title} ออนไลน์ฟรีได้เลยที่ {SiteName} ไม่ต้องดาวน์โหลด"
            : $"{snippet} เล่น {g.Title} ออนไลน์ฟรีได้เลยที่ {SiteName} ไม่ต้องดาวน์โหลด";
    }

    public static string HomeJsonLd() => """
        {
          "@context": "https://schema.org",
          "@type": "WebSite",
          "name": "KiddoGame",
          "url": "https://kiddogame.net",
          "description": "รวมเกมออนไลน์สำหรับเด็กฟรีกว่า 2,000 เกม เล่นได้เลยบนเบราว์เซอร์ ไม่ต้องดาวน์โหลด",
          "inLanguage": "th",
          "potentialAction": {
            "@type": "SearchAction",
            "target": {
              "@type": "EntryPoint",
              "urlTemplate": "https://kiddogame.net/?search={search_term_string}"
            },
            "query-input": "required name=search_term_string"
          }
        }
        """;

    public static string GameJsonLd(Game g, string url)
    {
        var desc  = !string.IsNullOrEmpty(g.DescriptionTh) ? g.DescriptionTh
                  : g.Description ?? g.Title;
        var image = g.ThumbnailUrl ?? DefaultOgImage;
        return $$"""
            {
              "@context": "https://schema.org",
              "@type": "SoftwareApplication",
              "applicationCategory": "GameApplication",
              "name": "{{Esc(g.Title)}}",
              "description": "{{Esc(desc)}}",
              "image": "{{image}}",
              "url": "{{url}}",
              "operatingSystem": "Web Browser",
              "inLanguage": "th",
              "offers": {
                "@type": "Offer",
                "price": "0",
                "priceCurrency": "THB"
              }
            }
            """;
    }
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
dotnet test webapp.Tests/ --filter "FullyQualifiedName~SeoMetaTests"
```

Expected: 7 PASS

- [ ] **Step 5: Commit**

```bash
git add webapp/Services/SeoMeta.cs webapp.Tests/SeoMetaTests.cs
git commit -m "feat: add SeoMeta static helper with Thai SEO strings and JSON-LD builders"
```

---

### Task 2: SeoHead.razor + App.razor title fix

**Files:**
- Create: `webapp/Components/Shared/SeoHead.razor` (create the `Shared/` folder too)
- Modify: `webapp/Components/App.razor`

No unit tests for Razor components — verify with `dotnet build`.

- [ ] **Step 1: Create Shared directory**

```bash
mkdir -p webapp/Components/Shared
```

- [ ] **Step 2: Create SeoHead.razor**

```razor
@* webapp/Components/Shared/SeoHead.razor *@
<PageTitle>@Title</PageTitle>
<HeadContent>
    <meta name="description" content="@Description" />
    <link rel="canonical" href="@Canonical" />

    <meta property="og:type"        content="@OgType" />
    <meta property="og:title"       content="@Title" />
    <meta property="og:description" content="@Description" />
    <meta property="og:url"         content="@Canonical" />
    <meta property="og:site_name"   content="KiddoGame" />
    <meta property="og:locale"      content="th_TH" />
    @if (!string.IsNullOrEmpty(OgImage))
    {
        <meta property="og:image" content="@OgImage" />
    }

    <meta name="twitter:card"        content="summary_large_image" />
    <meta name="twitter:title"       content="@Title" />
    <meta name="twitter:description" content="@Description" />
    @if (!string.IsNullOrEmpty(OgImage))
    {
        <meta name="twitter:image" content="@OgImage" />
    }

    <link rel="alternate" hreflang="th"        href="@Canonical" />
    <link rel="alternate" hreflang="en"        href="@Canonical" />
    <link rel="alternate" hreflang="x-default" href="@Canonical" />

    @if (!string.IsNullOrEmpty(JsonLd))
    {
        <script type="application/ld+json">@((MarkupString)JsonLd)</script>
    }
</HeadContent>

@code {
    [Parameter, EditorRequired] public string  Title       { get; set; } = "";
    [Parameter, EditorRequired] public string  Description { get; set; } = "";
    [Parameter, EditorRequired] public string  Canonical   { get; set; } = "";
    [Parameter] public string  OgType  { get; set; } = "website";
    [Parameter] public string? OgImage { get; set; }
    [Parameter] public string? JsonLd  { get; set; }
}
```

- [ ] **Step 3: Remove static `<title>` from App.razor**

In `webapp/Components/App.razor`, delete the line:
```html
    <title>KiddoGame</title>
```

The `<HeadOutlet />` already in the file handles all dynamic titles through `<PageTitle>`.

- [ ] **Step 4: Verify build passes**

```bash
dotnet build webapp/
```

Expected: Build succeeded, 0 errors

- [ ] **Step 5: Commit**

```bash
git add webapp/Components/Shared/SeoHead.razor webapp/Components/App.razor
git commit -m "feat: add SeoHead shared component, remove static title from App.razor"
```

---

### Task 3: Home.razor — SeoHead + visually-hidden Thai H1

**Files:**
- Modify: `webapp/Components/Pages/Home.razor`

- [ ] **Step 1: Add SeoHead and H1 at the top of Home.razor**

Insert these two lines immediately after the `@implements IDisposable` line (before the `<!-- Search Bar -->` comment):

```razor
<SeoHead Title="@SeoMeta.HomeTitle"
         Description="@SeoMeta.HomeDescription"
         Canonical="@(SeoMeta.BaseUrl + "/")"
         JsonLd="@SeoMeta.HomeJsonLd()" />

<h1 class="sr-only">@SeoMeta.HomeH1</h1>
```

The top of the file should look like this after the edit:

```razor
@page "/"
@rendermode InteractiveServer
@inject GameService GameSvc
@inject LanguageService LangSvc
@implements IDisposable

<SeoHead Title="@SeoMeta.HomeTitle"
         Description="@SeoMeta.HomeDescription"
         Canonical="@(SeoMeta.BaseUrl + "/")"
         JsonLd="@SeoMeta.HomeJsonLd()" />

<h1 class="sr-only">@SeoMeta.HomeH1</h1>

<!-- Search Bar -->
```

The `sr-only` Tailwind class makes the H1 visible to search engines but not to users, keeping the layout unchanged.

- [ ] **Step 2: Verify build passes**

```bash
dotnet build webapp/
```

Expected: Build succeeded, 0 errors

- [ ] **Step 3: Commit**

```bash
git add webapp/Components/Pages/Home.razor
git commit -m "feat: add SeoHead and sr-only Thai H1 to Home page"
```

---

### Task 4: GamePage.razor — dynamic SeoHead per game

**Files:**
- Modify: `webapp/Components/Pages/GamePage.razor`

- [ ] **Step 1: Add SeoHead to each render branch**

In `webapp/Components/Pages/GamePage.razor`, make three targeted edits:

**Edit A** — inside the `@if (_loading)` block, add SeoHead as the very first child:

```razor
@if (_loading)
{
    <SeoHead Title="KiddoGame - เกมเด็กออนไลน์ฟรี"
             Description="@SeoMeta.HomeDescription"
             Canonical="@(SeoMeta.BaseUrl + "/games/" + Slug)" />
    <div class="flex justify-center items-center min-h-[60vh]">
```

**Edit B** — inside the `else if (_game is null)` block, add SeoHead as the very first child:

```razor
else if (_game is null)
{
    <SeoHead Title="ไม่พบเกม | KiddoGame"
             Description="@SeoMeta.HomeDescription"
             Canonical="@(SeoMeta.BaseUrl + "/games/" + Slug)" />
    <div class="flex justify-center items-center min-h-[60vh]">
```

**Edit C** — inside the `else` block, add SeoHead before the `var description = ...` line:

```razor
else
{
    <SeoHead Title="@SeoMeta.GameTitle(_game)"
             Description="@SeoMeta.GameDescription(_game)"
             Canonical="@(SeoMeta.BaseUrl + "/games/" + _game.Slug)"
             OgType="article"
             OgImage="@(_game.ThumbnailUrl ?? SeoMeta.DefaultOgImage)"
             JsonLd="@SeoMeta.GameJsonLd(_game, SeoMeta.BaseUrl + "/games/" + _game.Slug)" />

    var description = LangSvc.Current == Lang.Thai && !string.IsNullOrEmpty(_game.DescriptionTh)
        ? _game.DescriptionTh : _game.Description;
```

- [ ] **Step 2: Verify build passes**

```bash
dotnet build webapp/
```

Expected: Build succeeded, 0 errors

- [ ] **Step 3: Commit**

```bash
git add webapp/Components/Pages/GamePage.razor
git commit -m "feat: add dynamic SeoHead to GamePage with OG tags and JSON-LD"
```

---

### Task 5: GetAllSlugsAsync + /sitemap.xml endpoint

**Files:**
- Modify: `webapp/Services/GameService.cs`
- Modify: `webapp/Program.cs`

- [ ] **Step 1: Add GetAllSlugsAsync to GameService.cs**

Add these two members immediately before the existing `private record CategoryRow(string Category, int Count);` line at the bottom of the class:

```csharp
    public async Task<List<string>> GetAllSlugsAsync()
    {
        var json = await http.GetStringAsync("games?select=slug&status=eq.done&limit=10000");
        var rows = JsonSerializer.Deserialize<List<SlugRow>>(json, _json) ?? [];
        return rows.Select(r => r.Slug).ToList();
    }

    private record SlugRow(string Slug);
```

- [ ] **Step 2: Add /sitemap.xml endpoint to Program.cs**

Add before `app.Run()`:

```csharp
app.MapGet("/sitemap.xml", async (GameService gameSvc) =>
{
    List<string> slugs;
    try { slugs = await gameSvc.GetAllSlugsAsync(); }
    catch { return Results.StatusCode(503); }

    var sb = new System.Text.StringBuilder();
    sb.AppendLine("<?xml version=\"1.0\" encoding=\"UTF-8\"?>");
    sb.AppendLine("<urlset xmlns=\"http://www.sitemaps.org/schemas/sitemap/0.9\">");

    sb.AppendLine("  <url>");
    sb.AppendLine("    <loc>https://kiddogame.net/</loc>");
    sb.AppendLine("    <changefreq>daily</changefreq>");
    sb.AppendLine("    <priority>1.0</priority>");
    sb.AppendLine("  </url>");

    foreach (var slug in slugs)
    {
        sb.AppendLine("  <url>");
        sb.AppendLine($"    <loc>https://kiddogame.net/games/{slug}</loc>");
        sb.AppendLine("    <changefreq>monthly</changefreq>");
        sb.AppendLine("    <priority>0.8</priority>");
        sb.AppendLine("  </url>");
    }

    sb.AppendLine("</urlset>");
    return Results.Content(sb.ToString(), "application/xml");
});
```

Note: `GameService` is registered as a typed HTTP client (transient), so it injects cleanly into the minimal API handler per-request.

- [ ] **Step 3: Verify build passes**

```bash
dotnet build webapp/
```

Expected: Build succeeded, 0 errors

- [ ] **Step 4: Commit**

```bash
git add webapp/Services/GameService.cs webapp/Program.cs
git commit -m "feat: add GetAllSlugsAsync and /sitemap.xml minimal API endpoint"
```

---

### Task 6: robots.txt

**Files:**
- Create: `webapp/wwwroot/robots.txt`

- [ ] **Step 1: Create robots.txt**

```
User-agent: *
Allow: /
Disallow: /Error
Sitemap: https://kiddogame.net/sitemap.xml
```

This file is served automatically by `app.UseStaticFiles()` already in `Program.cs`.

- [ ] **Step 2: Verify build passes**

```bash
dotnet build webapp/
```

Expected: Build succeeded, 0 errors

- [ ] **Step 3: Commit**

```bash
git add webapp/wwwroot/robots.txt
git commit -m "feat: add robots.txt with sitemap directive"
```

---

### Task 7: Run full test suite and verify

- [ ] **Step 1: Run all tests**

```bash
dotnet test webapp.Tests/
```

Expected: All tests pass. Total should include 7 new SeoMetaTests plus all prior tests.

- [ ] **Step 2: Manual verification checklist**

Start the dev server:
```bash
dotnet run --project webapp/
```

Then verify in a browser or with curl (Blazor Server pre-renders on first request, so `curl` captures the static HTML):

```bash
# Title on home page
curl -s http://localhost:5000/ | grep -o '<title>[^<]*</title>'
# Expected: <title>KiddoGame - เกมออนไลน์สำหรับเด็กฟรี 2,000+ เกม ไม่ต้องดาวน์โหลด</title>

# Meta description on home page
curl -s http://localhost:5000/ | grep 'name="description"'
# Expected: <meta name="description" content="รวมเกมออนไลน์..." />

# Thai H1 on home page
curl -s http://localhost:5000/ | grep 'sr-only'
# Expected: <h1 class="sr-only">เกมออนไลน์สำหรับเด็กฟรี เล่นได้เลยไม่ต้องดาวน์โหลด</h1>

# robots.txt
curl -s http://localhost:5000/robots.txt
# Expected: User-agent: * ... Sitemap: https://kiddogame.net/sitemap.xml

# sitemap.xml first 10 lines
curl -s http://localhost:5000/sitemap.xml | head -10
# Expected: <?xml version="1.0" ... <urlset ... <loc>https://kiddogame.net/</loc>

# Game page title (replace slug with a real one from the DB)
curl -s 'http://localhost:5000/games/subway-surfers' | grep -o '<title>[^<]*</title>'
# Expected: <title>Subway Surfers - เล่นออนไลน์ฟรี | เกมเด็ก KiddoGame</title>
```

- [ ] **Step 3: Validate JSON-LD**

Open Google's Rich Results Test at https://search.google.com/test/rich-results and test:
- `https://kiddogame.net/` — should show WebSite + SearchAction
- `https://kiddogame.net/games/<any-slug>` — should show SoftwareApplication

- [ ] **Step 4: Final commit (if any cleanup needed)**

```bash
git add -p
git commit -m "chore: SEO implementation complete"
```
